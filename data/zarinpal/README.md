# ZarinPal Data Boundary

Place the official challenge dataset in this directory for local use. Raw files and generated DuckDB databases are intentionally ignored. Do not add synthetic merchant insights.

When the dataset arrives, copy `metadata.example.json`, record its exact filename, SHA-256 checksum, byte size, row count, column profile, and ingestion timestamp, then create feature 007 from the observed schema.
