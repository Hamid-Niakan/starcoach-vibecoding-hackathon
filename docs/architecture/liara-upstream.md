# Liara Documentation Upstream

- Repository: `https://github.com/liara-cloud/docs.git`
- Branch: `master`
- Imported commit: `dbb7430b`
- Import method: `git subtree add --prefix=apps/liara-docs ... --squash`
- Imported: 2026-08-20

The monorepo changes the package-manager boundary and adds the shared assistant UI, but preserves the documentation content and visual system. Use the root README update command from a clean working tree and review upstream package/configuration changes before resolving workspace adaptations.
