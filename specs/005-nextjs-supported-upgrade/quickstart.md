# Quickstart: Supported Next.js Upgrade

```bash
pnpm install --frozen-lockfile
pnpm build
pnpm lint
pnpm type-check
pnpm test
pnpm release:check
```

The final command is expected to fail before this feature and must pass afterward.
