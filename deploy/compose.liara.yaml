services:
  liara:
    build:
      context: ..
      dockerfile: apps/liara-docs/Dockerfile.monorepo
      args:
        {
          NEXT_PUBLIC_AI_GATEWAY_URL: "${NEXT_PUBLIC_AI_GATEWAY_URL:?set NEXT_PUBLIC_AI_GATEWAY_URL}",
        }
    ports: ["8080:8080"]
    restart: unless-stopped
