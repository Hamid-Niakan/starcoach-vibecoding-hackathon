# Quickstart: Shared Chat Skeleton

Start PostgreSQL and the API, then both frontends. Create a message in each product, observe multiple delta events, reload, and verify history. Attempt to load the Liara conversation through the ZarinPal endpoint and expect 404. Cancel an active stream and verify a recoverable UI state.
