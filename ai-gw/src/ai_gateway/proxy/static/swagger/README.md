# Swagger UI assets

These Swagger UI assets are vendored from the repository's pinned LiteLLM2 reference tree. They are served locally so `/docs` makes no CDN request. When updating, copy the matching bundle, CSS, and favicon together, review the upstream license/version header, run the Swagger contract tests, and update `uv.lock` only if Python dependencies changed.
